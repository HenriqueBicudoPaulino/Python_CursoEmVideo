#Make a program that reads an integer and then displays its successor and predecessor

num = int(input('Type a integer value: '))

print('Analyzing this value, it\'s predecessor is {} and it\'s successor is {}'.format(num - 1, num + 1))
